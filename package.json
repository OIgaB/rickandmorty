{
  "name": "rickandmorty",
  "version": "1.0.0",
  "description": "Backend and Frontend for visualizing Rick and Morty characters",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "front-dev": "npm run dev -w frontend",
    "front-build": "npm run build -w frontend",
    "front-start": "npm run start -w frontend",
    "front-lint": "npm run lint -w frontend",

    "back-dev": "npm run start:dev -w backend",
    "back-build": "npm run build -w backend",
    "back-start": "npm run start:prod -w backend",
    "back-lint": "npm run lint -w backend",

    "db-generate": "npm run prisma generate -w backend",
    "db-seed": "npm run prisma db seed -w backend",
    "db-studio": "npm run prisma studio -w backend",
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ]
}
